{
	"name": "cat",
	"version": "0.0.0",
	"devDependencies": {
		"less": "latest",
		"watch": "latest"
	},
	"scripts": {
		"css": "lessc public/less/main.less > public/css/main.css",
		"css:watch": "watch 'npm run css' public/less/",
		"build": "npm run css",
		"build:watch": "npm run css:watch"
	}
}
